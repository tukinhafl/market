version: '3.9'

services:
  db:
    container_name: db
    image: postgres
    environment:
      - POSTGRES_USER=arthur
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=kenzie_market
    ports:
      - 5432:5432